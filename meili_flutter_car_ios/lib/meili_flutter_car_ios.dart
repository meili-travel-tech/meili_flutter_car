/// iOS implementation of the `meili_flutter` plugin.
///
/// This package contributes only the native iOS layer (see `ios/`). The Dart
/// surface is shared: it uses the default `MethodChannelMeiliFlutter` from
/// `meili_flutter_platform_interface`, so there is no per-platform Dart class
/// to register here (no `dartPluginClass` in `pubspec.yaml`).
library meili_flutter_ios;
