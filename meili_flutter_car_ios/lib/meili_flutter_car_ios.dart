/// iOS implementation of the `meili_flutter_car` plugin.
///
/// This package contributes only the native iOS layer (see `ios/`). The Dart
/// surface is shared: it uses the default `MethodChannelMeiliCarFlutter` from
/// `meili_flutter_car_platform_interface`, so there is no per-platform Dart
/// class to register here (no `dartPluginClass` in `pubspec.yaml`).
library meili_flutter_car_ios;
